<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thebipolaroptimist.projecttwo">

    <application
        android:name=".ProjectTwo"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EntryCalendarActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EntryListActivity"
            android:label="Entry List:"/>
        <activity android:name=".SummaryActivity" />
        <activity
            android:name=".SettingsActivity"
            android:theme="@style/AppTheme.ActionBar" />

        <activity android:name=".EntryCreateActivity"
            android:parentActivityName=".EntryCalendarActivity"
            android:theme="@style/AppTheme.ActionBar"
            android:windowSoftInputMode="stateUnchanged"
            android:label="Create Entry:"></activity>
    </application>

</manifest>